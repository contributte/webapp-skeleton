# Parameters
#
parameters:

	# System
	system:
		error:
			email: dev@contributte.org
			presenter: Front:Error

	# Database
	database:
		driver: pdo_pgsql
		port: 5432

	# Project
	project:
		rev: "1.0.0"

	# File
	files:
		root: %appDir%/../data/files

	# Mailing
	mailing:
		from: nutella@localhost
		from_name: Nutella

	# STMP
	smtp:
		host: localhost
		username: fake
		password: fake
